---
name: check-compiler-errors
description: Run compile and type-check commands and report failures
---

# Check compiler errors

Run `npm install` then `npm run build` (and `npm run dev` smoke if practical). Fix type/build errors until clean or document blocker in PR.
